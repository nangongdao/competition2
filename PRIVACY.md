# 隐私与数据处理说明（Privacy & Data Handling）

> 适用产品：AI 同声传译助手（competition2）
>
> 本文档说明本应用如何处理用户音频、文本与配置数据。当前产品形态为
> **本地优先（local-first）桌面应用**，数据默认留在本机，安全与隐私是
> 核心差异化卖点。

---

## 一、数据类别与去向

| 数据类别 | 说明 | 存储位置 | 是否上云 |
|---|---|---|---|
| 音频流 | 麦克风/系统音频/文件/标签页采集的实时音频 | 内存中临时缓冲（`audio_ttl_seconds`，默认 120s 后清理） | **取决于 ASR 引擎** |
| ASR 识别文本 | 音频转写的文字结果 | 会话上下文（Redis，`segment_ttl_seconds`，默认 300s） | **取决于 ASR/NMT 引擎** |
| 翻译结果 | 目标语言译文 | 会话上下文、可选导出文件 | **取决于 NMT 引擎** |
| 术语表 | 用户导入的术语条目 | 本地 JSON（`config/glossary.local.json`） | 否 |
| 翻译记忆库 | 已翻译句对 | 本地 JSON（`config/translation-memory.local.json`） | 否 |
| 会话历史 | 会话台账（开始时间/句数/时长/质量指标） | 本地 JSON（`config/session-history.local.json`） | 否 |
| 订阅/成本 | 套餐与用量统计 | 本地 JSON | 否 |
| 本地设置 | 桌面设置、字幕样式 | 本地 JSON | 否 |

**关键点**：所有持久化数据均保存在用户本机 `config/` 目录，不上传自有服务器。

---

## 二、哪些数据会发送到第三方

本应用默认依赖 **第三方 AI 引擎** 完成实时转写（ASR）与翻译（NMT）：

- **远程 ASR**：音频片段会上传到所配置的 OpenAI 兼容语音识别服务
  （默认 OpenAI Whisper）。**切换本地 Whisper（`ASR_ENGINE=whisper`）后音频不出本机。**
- **远程 NMT**：识别文本与上下文会上传到所配置的 LLM 翻译服务
  （默认 Anthropic Claude / OpenAI）。**目前 NMT 依赖远程 LLM；本地离线 NMT 为远期规划。**
- **TTS**：默认 `edge` 引擎调用 Microsoft Edge TTS 在线服务合成语音；
  切换为 `off` 则不发起任何语音合成请求。

> ⚠️ 只要使用第三方引擎，涉及的一段音频/文本就会离开本机，
> 请勿用本应用处理含商业秘密或个人隐私的敏感内容，除非已配置完全本地化方案。

---

## 三、本地化（隐私优先）配置建议

为最大限度保护隐私，建议按需关闭远程调用：

```bash
# ASR：完全本地（音频不出本机）
ASR_ENGINE=whisper
WHISPER_MODEL=small
WHISPER_DEVICE=cpu

# NMT：仍为远程 LLM（当前无本地 NMT 引擎）

# TTS：完全离线（不发起在线语音合成）
TTS_ENGINE=off
```

---

## 四、安全措施（默认启用）

- **默认仅监听 `127.0.0.1`**：应用服务不向局域网/公网暴露。
- **CORS 白名单**：仅放行本机前端来源，禁止通配符。
- **WebSocket 鉴权**：Origin 校验 + 强随机会话 ID + 重连令牌。
- **防 XFF 绕过**：禁用 `X-Forwarded-*` 覆写，loopback 校验不可绕过。
- **敏感文件 0600 权限**：本地设置、API key 以受限权限原子写入。
- **密钥不落库**：`.env` 被 `.gitignore` 排除，只有 `.env.example` 模板入库。

---

## 五、用户权利

- **查看/删除**：会话历史面板可查看、单删、清空全部会话记录。
- **清理过期**：会话台账默认 TTL 7 天，启动自动清理过期记录。
- **导出产物**：字幕可导出 TXT/SRT/VTT/Markdown，导出内容由用户自行管理。

---

## 六、合规提示

在将本应用用于商业或对外服务前，建议：

1. 发布明确的**隐私政策**与**数据处理协议**；
2. 若切换为云端 SaaS 形态，建立账号体系、访问控制与审计日志；
3. 与第三方 AI 提供商确认数据处理条款（数据是否用于训练、保留时长等）；
4. 获取必要的用户授权同意（尤其涉及音频数据时）。

---

*最后更新：2026-08-10*
